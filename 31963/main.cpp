#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // TODO: implement the algorithm for problem 31963 once the
    // problem statement is available. Currently this is a placeholder
    // because network restrictions prevent retrieving the task.

    return 0;
}
